# Google Colab dependencies — ML training only.
# Colab already ships most of these; pin scikit-learn to match the laptop so the
# exported model.pkl loads cleanly on the laptop side.

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3      # KEEP THIS ALIGNED WITH requirements.txt
matplotlib>=3.7        # evaluation plots (confusion matrix, feature importance)
joblib>=1.3            # export the trained model as model.pkl

# In Colab, install with:
#   !pip install -r requirements-colab.txt
